{% render 'hero' with {
settings: section.settings,
id: section.id

} %}
{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "heading",
      "default": "Schemas Loaded Correctly"
    }
  ],
  "presets": [
    {
      "name": "Hero",
      "category": "Image w/ text"
    }
  ]
}
{% endschema %}